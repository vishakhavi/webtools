{
  "name": "express-login",
  "version": "1.0.0",
  "description": "* Start from the up-to-date main branch (`git checkout main; git pull origin main`)\r * Create a feature branch named 'express-login' (`git checkout -b express-login`)\r * Create a package.json and necessary files to complete the work described below\r * Add, commit, and push the branch to github\r * Create a PR to merge to main\r * Be sure to include the reviewer(s).  \r * Due by Mon Jun 13 11:59pm (ET)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.18.1",
    "uuid": "^8.3.2"
  }
}
